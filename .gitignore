# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Environment variables
.env
.env.local
.env.*.local

# Jupyter Notebook
.ipynb_checkpoints

# pytest
.pytest_cache/
.coverage
htmlcov/

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Napari/Qt
*.qss

# Node.js / TUI
gently/tui/node_modules/
gently/tui/dist/

# Database
*.db
*.sqlite
*.sqlite3

# Test scripts (one-off tests in repo root, not tests/ directory)
/test_*.py
/check_*.py

# SAM model weights
*.pth

# Binary data
*.tif
!docs/*.tif
*.npz
*.gif

# Test images
*_centering_*.png
*_marked_*.png
analysis_*.png
embryo_*.png
initial_view_*.png
comparison_*.png
sam_*.png
spim_*.png
*_montage.png
*_analysis.png
bottom_camera_*.png

# HuggingFace benchmark data (large volumes)
benchmarks/data/hf-benchmark/

# Output directories (experiment data, calibration artifacts)
experiment_data/
dispim_data/
lightsheet_captures/

# Temporary JSON data
dispim_baseline_state_*.json
dispim_parameters_*.json

# Windows artifacts
nul
*.bak

# Local settings
.claude/settings.local.json
*.local.json

# Backup files
*_backup.py
*_backup/

# Mesh networking runtime artifacts (generated at startup, NEVER commit keys)
config/mesh_*.pem
config/mesh_key.pem
config/mesh_cert.pem
config/mesh_instance_id
config/mesh_audit.jsonl
config/mesh_trusted_peers.json

# LaTeX build artifacts
*.aux
*.out
*.toc
paper/*.log
paper/*.pdf
paper/*.synctex.gz

# Dead Electron app
electron/

# Root-level one-off documents
/stage_definitions_for_review.pdf
/stage_definitions_for_review.txt
gently/ui/tui/node_modules/
gently/ui/tui/dist/

# Stray local storage: GENTLY_STORAGE_PATH default (D:\Gently3) resolves
# literally to ./D:/ under the repo on Linux. Not data we track.
/D:/
.superpowers/

# Runtime operator overrides (written by the Settings panel)
config/config.local.yml
config/settings.local.yml
config/dashboard_defaults.json

# UI crawler generated output
tools/ui_crawler/out*/

